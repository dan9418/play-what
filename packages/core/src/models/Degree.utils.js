const getName = (data) => data;
const getPreview = (data) => data;
const getPodAtPitch = () => null;
const getMetaChildren = () => null;

export default {
	getName,
	getPreview,
	getPodAtPitch,
	getMetaChildren
}
